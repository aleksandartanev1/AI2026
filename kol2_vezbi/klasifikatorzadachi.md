# Рецепт за задачите (sklearn) — колоквиум 2

Идејата: **секоја задача е ист скелет**, само со различни зачини. Научи го скелетот еднаш, потоа само гледаш што бара конкретната задача.

---

## 🔁 СКЕЛЕТ (важи за сите задачи)

```python
# 1. Земи го dataset-от (даден е, не го пипаш)

# 2. Прочитај го инпутот (input())
x = int(input())

# 3. Раздели dataset на X (features) и Y (label)
X = [row[:-1] for row in dataset]   # сѐ освен последната колона
Y = [row[-1] for row in dataset]    # последна колона = класа

# 4. Подели train/test
split = len(X) * x // 100      # или *0.85, зависи од задачата
train_X, test_X = X[:split], X[split:]
train_Y, test_Y = Y[:split], Y[split:]

# 5. Направи класификатор, фитувај
classifier = НекојКласификатор(...)
classifier.fit(train_X, train_Y)

# 6. Резултат
acc = classifier.score(test_X, test_Y)
pred = classifier.predict([record])
```

Ако го знаеш ова напамет — 70% од задачата е готова секогаш.

---

## 🧠 Кои класификатори ги имаш и кога се користат

Не мора да разбираш математика зад нив — само запомни "етикета":

| Класификатор | Import | Кратко "паметило" |
|---|---|---|
| `GaussianNB` | `sklearn.naive_bayes` | Брз, наивен, добар за старт |
| `DecisionTreeClassifier` | `sklearn.tree` | Дрво на одлуки, има `feature_importances_` |
| `RandomForestClassifier` | `sklearn.ensemble` | "Многу дрвца гласаат" — `n_estimators` = колку дрвца |
| `MLPClassifier` | `sklearn.neural_network` | Невронска мрежа, треба SKALIRANJE пред неа! |

**Правило со ѕвездичка:** MLP (невронска мрежа) речиси секогаш бара `MinMaxScaler` или `StandardScaler` пред неа. Другите (Bayes, Tree, Forest) — не им треба скалирање.

---

## 🪜 Чекор-по-чекор кога отвораш нова задача

1. **Прочитај го крајот на `if __name__`** – таму гледаш точно што inputs бара (`input()` редоследот) и што печати. Тоа ти го кажува "плаот" на задачата.
2. **Погледни ја последната колона на dataset** – тоа е Y (класата/label).
3. **Прашај се: split е по проценти или по фиксен број?** Секогаш е некаков `len(...) * x // 100` или `int(len(...)*x)`.
4. **Дали задачата бара по-класа split** (пр. 13.py, 15.py)? Тогаш прво раздели dataset по класи, па подели секоја класа посебно train/test, па спои ги пак со `+`.
5. **Дали има скалирање?** Ако користиш MLP → задолжително `fit_transform` на train, `transform` на test (никогаш fit на test!).
6. **Дали бара да отфрлиш колона?** (пр. 4.py col_index, 10.py worst_index) — тоа е `row[:i] + row[i+1:]`.
7. **На крај — printaj точно она што се бара**, во точен формат (`f"..."`).

---

## ⚠️ Најчести стапици (истите се повторуваат)

- **fit vs transform**: Scaler/Encoder секогаш `fit_transform` на train, само `transform` на test. Никогаш fit на test!
- **Редослед на split**: некои задачи прво сечат `train, test`, некои `test, train` (внимавај на редот во кодот — не претпоставувај).
- **`record` од input** — секогаш е lista од бројки, но класификаторот бара `[[...]]` (листа во листа) кога правиш `predict()` за еден ред.
- **Отфрлена колона важи и за `record`** — ако бришеш колона од dataset, мора да ја избришеш и од record пред predict.
- **Гласање (ensemble/voting)** — кога комбинираш повеќе модели рачно (пр. 13.py, 15.py, 16.py): секој модел прави `predict()`, ги бројат гласовите, класата со најмногу гласови победува.

---

## 🎯 Мал трик за учење

Наместо да учиш секоја задача одделно — секогаш прашај си 4 прашања:
1. Каде е X, каде е Y?
2. Како се дели train/test?
3. Кој класификатор + треба ли скалирање?
4. Што точно бара да испечати?

Ако одговориш на овие 4, задачата е решена.